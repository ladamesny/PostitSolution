<div class="row">
  <!-- Voting feature -->
  <% if logged_in? %>
  <div class="span0 well text-center">
    <%= link_to vote_post_path(post, vote: true), method: 'post' do %>
      <i class="icon-arrow-up"></i>
    <% end %>
    <br/>
    <%= post.total_votes %> votes
    <br/>
   <%= link_to vote_post_path(post, vote: false), method: 'post' do %>
      <i class="icon-arrow-down"></i>
    <% end %>
  </div>
  <% end %>

  <!-- Actual Post information -->
  <div class="span8 well">
    <%= render post.categories %>
    <h4><%= link_to post.title, fix_url(post.url), target: '_blank' %></h4>
    <p><%= post.description %></p>
    <p>
      <span class="quiet">posted by</span> <%= link_to post.creator.username, user_path(post.creator) %> <small> at <%=display_datetime(post.created_at)%></small>
    </p>
    <span><%= link_to(pluralize(post.comments.count, 'comment'), post_path(post)) %></span>
  </div>
</div>